/* In this script, we are gooing to learn about incerementand decrement operators */

// prefix 

let counter = 1;
let a = ++counter; // (*)

alert(a); // 2

// postfix

let counter1 = 1;
let b = counter1++; // (*) // here the postfix operator increse the value but give the old value  

alert(b); // 1

